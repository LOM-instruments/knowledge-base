/*
 * lom-enhancements.js
 *
 * The top-tab mega-menu (the "+" marker and hover dropdowns) is now rendered
 * entirely server-side by overrides/partials/tabs.html, so this file no
 * longer builds or modifies the tabs. Kept as a no-op placeholder; add any
 * future client-side enhancements here.
 */
(function () {
  /* intentionally empty */
})();
